import { CheckCheck, ShieldCheck } from 'lucide-react';
export default function CoachPanel({ records, onApprove }) {
  const recent = [...records].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt));
  return <section className="pixel-card"><div className="mb-4 flex items-center justify-between"><div><div className="badge">COACH VERIFY</div><h3 className="mt-2 text-xl font-bold text-white">최근 제출 기록</h3></div><ShieldCheck size={18} className="text-gold" /></div><div className="space-y-3">{recent.map((record)=><div key={record.id} className="rounded-2xl border border-line bg-panelSoft p-4"><div className="mb-3 flex items-start justify-between gap-3"><div><p className="font-bold text-white">{record.memberName} · {record.wodName}</p><p className="text-sm text-slate-300">{record.result} · {record.mode}</p><p className="mt-1 text-xs text-slate-400">{record.note || '메모 없음'}</p></div><span className={`rounded-full border px-3 py-1 text-xs font-bold ${record.official ? 'border-success/30 bg-success/10 text-success' : 'border-amber-300/30 bg-amber-300/10 text-amber-200'}`}>{record.official ? 'official' : 'unofficial'}</span></div><div className="flex items-center justify-between"><div className="text-xs text-slate-400">{new Date(record.createdAt).toLocaleString('ko-KR')}</div><button onClick={()=>onApprove(record.id)} disabled={record.official} className={`inline-flex items-center gap-2 rounded-2xl px-4 py-2 text-sm font-bold transition ${record.official ? 'cursor-not-allowed border border-line bg-slate-900 text-slate-500' : 'border border-success/40 bg-success/10 text-success hover:translate-y-[-1px]'}`}><CheckCheck size={16} />{record.official ? '승인 완료' : 'official 승인'}</button></div></div>)}</div></section>;
}

import { Crown, Medal } from 'lucide-react';
export default function RankingBoard({ weeklyRanking, prRanking, currentMemberId }) {
  return <div className="space-y-4"><section className="pixel-card"><div className="mb-4 flex items-center justify-between"><div><div className="badge">WEEKLY RANKING</div><h3 className="mt-2 text-xl font-bold text-white">박스 내 주간 랭킹</h3></div><Crown size={18} className="text-gold" /></div><div className="space-y-3">{weeklyRanking.length ? weeklyRanking.map((record, index)=><RankingRow key={record.id} index={index} record={record} highlight={record.memberId===currentMemberId} />) : <Empty text="이번 주 official 기록이 없습니다." />}</div></section><section className="pixel-card"><div className="mb-4 flex items-center justify-between"><div><div className="badge">PR BOARD</div><h3 className="mt-2 text-xl font-bold text-white">종목별 PR 랭킹</h3></div><Medal size={18} className="text-neon" /></div><div className="space-y-3">{prRanking.length ? prRanking.map((record, index)=><RankingRow key={record.id} index={index} record={record} highlight={record.memberId===currentMemberId} />) : <Empty text="공식 PR 기록이 없습니다." />}</div></section></div>;
}
function RankingRow({ index, record, highlight }) { return <div className={`flex items-center justify-between rounded-2xl border p-3 ${highlight ? 'border-neon bg-neon/10 shadow-glow' : 'border-line bg-panelSoft'}`}><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl border border-line bg-slate-950/60 font-bold text-white">#{index + 1}</div><div><p className="font-bold text-white">{record.memberName}</p><p className="text-xs text-slate-400">{record.wodName} · {record.mode} · {record.official ? 'official' : 'unofficial'}</p></div></div><div className="text-right"><p className="text-lg font-bold text-gold">{record.result}</p><p className="text-xs text-slate-400">{new Date(record.createdAt).toLocaleDateString('ko-KR')}</p></div></div>; }
function Empty({ text }) { return <div className="rounded-2xl border border-dashed border-line bg-panelSoft p-4 text-sm text-slate-400">{text}</div>; }
